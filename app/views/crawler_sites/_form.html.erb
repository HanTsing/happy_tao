<%= form_for(@crawler_site) do |f| %>
  <% if @crawler_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawler_site.errors.count, "error") %> prohibited this crawler_site from being saved:</h2>

      <ul>
      <% @crawler_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
